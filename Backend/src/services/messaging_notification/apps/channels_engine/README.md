WebSocket Consumers & Routing

L'application channels_engine gere les websocket et le routing. Elle est utilisée par l'application chat et l'application direct_messages. Elle utilise Redis pour la communication entre les différentes instances du serveur.