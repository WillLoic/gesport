Service d'authentification et de gestion des utilisateurs 

Fonctionnalités :
- gestion des utilisateurs, rôles (RBAC), tokens JWT / OAuth2, sessions, profils 

Technologies : Django + Django Ninja ou FastAPI (simple, robuste, support OAuth2/JWT natif) 

Base de données : PostgreSQL (auth_db)